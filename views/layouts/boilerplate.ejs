<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>StayGenoe</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
  <!-- Google Fonts -->
  <link
    href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap"
    rel="stylesheet">
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
    integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA=="
    crossorigin="anonymous" referrerpolicy="no-referrer" />
  <!-- Custom CSS -->
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/rating.css">
</head>

<body>

  <%- include('../includes/navbar') %>

  <main class="main-content-wrapper">
    <div class="container">
      <%- include('../includes/flash') %>
    </div>
    <%- body %>
  </main>

  <%- include('../includes/footer') %>

      <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"
        integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r"
        crossorigin="anonymous"></script>
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.min.js"
        integrity="sha384-BBtl+eGJRgqQAUMxJ7pMwbEyER4l1g+O15P+16Ep7Q9Q+zqX6gSbd85u4mG4QzX+"
        crossorigin="anonymous"></script>
      <script src="/js/script.js"></script>

      <!-- StayGenoe AI Concierge Widget -->
      <link rel="stylesheet" href="/css/chat.css">

      <button id="chatBtn" class="chat-btn" title="AI Concierge" aria-label="AI Concierge">
        <i class="fa-solid fa-wand-magic-sparkles"></i>
        <span class="chat-btn-pulse-dot"></span>
      </button>

      <div id="chatWindow" class="chat-window">
        <div class="chat-header">
          <div class="chat-header-info">
            <div class="chat-avatar-badge">
              <i class="fa-solid fa-sparkles"></i>
            </div>
            <div>
              <h5 class="chat-header-title">StayGenoe Concierge</h5>
              <p class="chat-header-status">
                <span class="chat-online-dot"></span> Online &bull; Instant AI
              </p>
            </div>
          </div>
          <button id="closeChat" class="close-chat" title="Close" aria-label="Close">&times;</button>
        </div>
        <div id="chatMessages" class="chat-messages">
          <div class="message bot">
            Hello! I'm your StayGenoe luxury travel assistant. How can I help you find your dream stay today?
            <div class="chat-quick-chips">
              <span class="chat-chip" onclick="handleChipClick('Find luxury villas in Santorini')">Santorini Villas</span>
              <span class="chat-chip" onclick="handleChipClick('How do I list my property?')">Host a Home</span>
              <span class="chat-chip" onclick="handleChipClick('Trending beach stays')">Beach Stays</span>
            </div>
          </div>
        </div>
        <div class="chat-input-area">
          <input type="text" id="chatInput" class="chat-input-control" placeholder="Ask anything about stays...">
          <button id="sendMessage" class="send-btn" title="Send message" aria-label="Send message">
            <i class="fa-solid fa-arrow-up"></i>
          </button>
        </div>
      </div>

      <script>
        const chatBtn = document.getElementById('chatBtn');
        const chatWindow = document.getElementById('chatWindow');
        const closeChat = document.getElementById('closeChat');
        const chatInput = document.getElementById('chatInput');
        const sendMessage = document.getElementById('sendMessage');
        const chatMessages = document.getElementById('chatMessages');

        // Toggle Chat Window
        chatBtn.addEventListener('click', () => {
          chatWindow.style.display = chatWindow.style.display === 'flex' ? 'none' : 'flex';
          if (chatWindow.style.display === 'flex') {
            chatInput.focus();
          }
        });

        closeChat.addEventListener('click', () => {
          chatWindow.style.display = 'none';
        });

        function handleChipClick(text) {
          chatInput.value = text;
          handleSendMessage();
        }

        // Send Message
        async function handleSendMessage() {
          const message = chatInput.value.trim();
          if (!message) return;

          // Append User Message
          appendMessage(message, 'user');
          chatInput.value = '';
          scrollBottom();

          // Show thinking state (optional)

          try {
            const response = await fetch('/chat', {
              method: 'POST',
              headers: {
                'Content-Type': 'application/json'
              },
              body: JSON.stringify({ message })
            });

            const data = await response.json();
            if (data.reply) {
              // Use typing effect for bot
              appendMessage(data.reply, 'bot', true);
            } else {
              appendMessage("Sorry, I didn't get that.", 'bot', true);
            }
          } catch (error) {
            console.error('Error:', error);
            appendMessage("Sorry, something went wrong.", 'bot', true);
          }
          // Scroll bottom is handled inside typeText now for bot
        }

        sendMessage.addEventListener('click', handleSendMessage);

        chatInput.addEventListener('keypress', (e) => {
          if (e.key === 'Enter') handleSendMessage();
        });

        function appendMessage(text, sender, isTyping = false) {
          const div = document.createElement('div');
          div.classList.add('message', sender);
          chatMessages.appendChild(div);

          if (sender === 'bot' && isTyping) {
            typeText(div, text);
          } else {
            div.innerText = text;
            scrollBottom();
          }
        }

        function typeText(element, text) {
          let index = 0;
          const interval = setInterval(() => {
            if (index < text.length) {
              element.innerText += text.charAt(index);
              index++;
              scrollBottom(); // Scroll as it types
            } else {
              clearInterval(interval);
            }
          }, 30); // Speed of typing in ms
        }

        function scrollBottom() {
          chatMessages.scrollTop = chatMessages.scrollHeight;
        }
      </script>
</body>

</html>